# Try to understand the GCD algorithm better, why and how it works. For that, include one or two print statements in the while-loop 


# Program to find the GCD of two integers.
num1 = int(input("Enter first number: "))
num2 = int(input("Enter second number: "))
if num2 > num1:
    num1, num2 = num2, num1 # make num1 > num2
n = 1
while n <= num2:
    if num1 % n == 0 and num2 % n == 0:
        gcd = n
    n += 1
print("The GCD of", num2, "and", num1, "is", gcd)
