# Program the Classes BankAccount, Student and OSU Student
# ModifyBankAccount, make sure that age is >= 18
# And modify to make sure that deposit is positive
# Implement BankAccountas dataclass


class Bank_Account:
    
    def __init__(self, FamilyName, FirstName, yearOfBirth):
        self.Balance = 0
        self.FamilyName = FamilyName
        self.FirstName = FirstName
        self.yearOfBirth = yearOfBirth

        print(f"Hello {self.FirstName} {self.FamilyName}! Welcome to the Bank_AccountClass")
    
    def deposit(self):
        while True:
            try:
                amount = float(input("Enter amount to be Deposited: "))
                if amount <= 0:
                    print("Invalid amount. Please enter a positive number.")
                else:
                    self.Balance += amount
                    print("\n ** You Deposited:", amount, "**")
                    break
            except ValueError:
                print("Invalid input. Please enter a valid number.")

    def withdraw(self):
        while True:
            try:
                amount = float(input("Enter amount to be Withdrawn: "))
                if amount > 0 and amount <= self.Balance:
                    self.Balance -= amount
                    print("\n ** You Withdrew:", amount, "**")
                    break
                elif amount <= 0:
                    print("Invalid amount. Please enter a positive number.")
                else:
                    print("\n Insufficient balance")
            except ValueError:
                print("Invalid input. Please enter a valid number.")
            
    def display(self):
        print("\n ** Net Available Balance =", self.Balance, "** \n")

    def run(self):
        while True:
            action = input("Enter 'D' to deposit or 'W' to withdraw: ")
            if action == 'D':
                self.deposit()
                self.display()
            elif action == 'W':
                self.withdraw()
                self.display()
            else:
                print("Invalid action. Please enter 'D' or 'W'.")        
   
familyName = input("Please enter your Family Name: ")
firstName = input("Please enter your First Name: ")
while True:
    yearOfBirth = int(input("Enter your Year of Birth: "))
    if yearOfBirth > 2005:
        print(f"{firstName}, Unfortunately you are still too young to open an account, sorry.")
    else:
        break
bankAccount = Bank_Account(familyName, firstName, yearOfBirth)
bankAccount.run()
